apiVersion: 1

providers:
  - name: 'RAG Services'
    orgId: 1
    folder: 'RAG'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

