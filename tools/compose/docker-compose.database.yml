name: database

# ==============================================================================
# TradingSystem - Shared Data Stack
# ==============================================================================
# This stack is currently empty. All data services have been moved to their
# respective application stacks or to the 3-database-stack (for UI tools).
#
# QuestDB was moved to 3-database-stack as it includes the web console UI.
#
# Each application now owns its dedicated database stack:
# - tools/compose/docker-compose.tp-capital-*.yml
# - tools/compose/docker-compose.workspace-simple.yml
# - tools/compose/docker-compose.4-2-telegram-stack.yml
# ==============================================================================

services:
  # No services currently in this stack

# ========================================
# Networks
# ========================================
networks:
  tradingsystem_backend:
    name: tradingsystem_backend
    external: true
