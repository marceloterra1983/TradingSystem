# Crawler Course Meta (app/)

Implementação de referência descrita na spec OpenSpec `crawler-course-meta`.

## Estrutura

```
app/crawler-course-meta/
├── backend  # API + CLI Node.js/TypeScript
└── frontend # Next.js 15 dashboard
```

Siga o README específico de cada subprojeto para executar localmente em conjunto com o `.env` da raiz do TradingSystem.
