# Dashboard Configuration

# Unified Domain Configuration (Recommended)
# Set to true to use single domain for all services (requires Nginx setup)
VITE_USE_UNIFIED_DOMAIN=true

# Base URL for all APIs when using unified domain
VITE_API_BASE_URL=http://localhost:3402
VITE_DOCUMENTATION_API_URL=http://localhost:3402
VITE_DOCS_URL=http://tradingsystem.local/docs

# Direct Port Access Configuration (Legacy)
# Only used when VITE_USE_UNIFIED_DOMAIN=false
# Workspace API
VITE_WORKSPACE_API_URL=http://localhost:3200/api/items
# VITE_TP_CAPITAL_API_URL=/api/tp-capital
# Comentado para usar o fallback do vite.config.ts (proxy para localhost:4007)
# NÃO defina esta variável para desenvolvimento local - deixe o código usar o fallback
# Firecrawl Proxy API URL
# Used by: firecrawlService.ts for web scraping operations
# Default: http://localhost:3600 (Firecrawl Proxy API)
VITE_FIRECRAWL_PROXY_URL=http://localhost:3600

# Telegram Gateway API (independente do Service Launcher)
VITE_TELEGRAM_GATEWAY_API_URL=http://localhost:4010
# Opcional: Token de acesso quando o backend exige cabeçalho X-Gateway-Token
# VITE_TELEGRAM_GATEWAY_API_TOKEN=gw_secret_example

# ============================================
# AGENTS PLATFORM CONFIGURATION
# ============================================

# Agents Scheduler API Base URL
# Default: http://localhost:8199
# Used by: agentsService.ts for scheduler API communication
# NOTE: Agents service remains on direct port access (external service)
VITE_AGENTS_SCHEDULER_URL=http://localhost:8199

# Agno Agents (FastAPI) base URL for UI integrations
VITE_AGNO_AGENTS_URL=http://localhost:8201

# LlamaIndex Query Service (FastAPI) base URL
# Default: http://localhost:8202
VITE_LLAMAINDEX_QUERY_URL=http://localhost:8202

# Optional: JWT for authenticating with the query service em modo direto
# O dashboard agora usa o proxy /api/v1/rag por padrão; preencha apenas se quiser forçar modo direct.
# Token válido até 29/10/2026 (dev-secret)
VITE_LLAMAINDEX_JWT=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJkYXNoYm9hcmQiLCJleHAiOjE3OTMyNDU1NjR9.XbMb7jduH5DP3ErWodUZ7AWSn0l02aKGr1_BTbH1vgs

# RAG Collections Service (standalone API, development only)
VITE_RAG_COLLECTIONS_URL=http://localhost:3403

# Environment
VITE_APP_ENV=development
