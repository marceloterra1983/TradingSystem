name: Agents Directory

on:
  pull_request:
    paths:
      - '.claude/agents/**'
      - 'scripts/agents/**'
      - 'frontend/dashboard/src/data/aiAgentsDirectory.ts'
      - 'package.json'
      - 'package-lock.json'
      - 'reports/agents/**'
      - '.github/workflows/agents-directory.yml'

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: agents-directory-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare Node workspace
        uses: ./.github/actions/setup-node
        with:
          node-version: '20'
          cache-dependency-path: package-lock.json

      - name: Run agents generator & tests
        run: npm run agents:ci

      - name: Ensure catalog files are committed
        run: git diff --exit-code
