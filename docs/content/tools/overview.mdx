---
title: Tools & Infrastructure
description: Development tools, infrastructure components, and automation assets for the TradingSystem platform
tags:
  - tools
  - infrastructure
  - devops
  - monitoring
  - ai
owner: ToolingGuild
lastReviewed: '2025-10-27'
---

# Tools & Infrastructure

Comprehensive toolkit for development, deployment, monitoring, and automation of the TradingSystem platform.

## 📚 Categories

### 🏗️ Infrastructure & Orchestration

#### Docker Compose (`tools/compose/`)
Docker Compose orchestration files for all platform services.

**Files:**
- `docker-compose.database.yml` - Database services (TimescaleDB, QuestDB, PostgreSQL, Adminer)
- `docker-compose.apps.yml` - Application services (Workspace, TP Capital, Documentation API, Firecrawl Proxy)
- `docker-compose.apps.prod.yml` - Production application configuration
- `docker-compose.docs.yml` - Documentation services (Docusaurus)
- `docker-compose.firecrawl.yml` - Firecrawl web scraping service
- `docker-compose.infrastructure.yml` - Core infrastructure
- `docker-compose.monitoring.yml` - Monitoring stack (Prometheus, Grafana, Alertmanager, Alert Router)
- `docker-compose.timescale.yml` - TimescaleDB standalone configuration
- `docker-compose.individual.yml` - Individual service testing

**Usage:**
```bash
# Start all databases
docker compose -f tools/compose/docker-compose.database.yml up -d

# Start monitoring stack
docker compose -f tools/monitoring/docker-compose.yml up -d

# Start all application services
docker compose -f tools/compose/docker-compose.apps.yml up -d
```

#### Nginx Reverse Proxy (`tools/nginx-proxy/`)
Nginx reverse proxy configurations for routing requests to backend services.

**Features:**
- Path-based routing
- Load balancing
- SSL/TLS termination (when configured)

#### Docker Registry (`tools/registry/`)
Private Docker registry for custom images.

#### Systemd Services (`tools/systemd/`)
Linux systemd service definitions for native services (non-containerized).

---

### 📊 Monitoring & Observability

#### Monitoring Stack (`tools/monitoring/`)

Comprehensive monitoring solution with Prometheus, Grafana, Alertmanager, and Alert Router.

**Components:**

1. **Prometheus** (Port 9090)
   - Metrics collection and storage
   - Alert rule evaluation
   - PromQL query engine
   - Config: `tools/monitoring/prometheus/prometheus.yml`
   - Rules: `tools/monitoring/prometheus/rules/`

2. **Grafana** (Port 3000)
   - Metrics visualization
   - Pre-configured dashboards
   - Dashboard provisioning
   - Dashboards: `tools/monitoring/grafana/dashboards/`
   - Config: `tools/monitoring/grafana/provisioning/`

3. **Alertmanager** (Port 9093)
   - Alert deduplication and routing
   - Notification management
   - Silencing and inhibition
   - Config: `tools/monitoring/alertmanager/`

4. **Alert Router** (Port 8080)
   - Prometheus alerts → GitHub Issues integration
   - Automatic issue lifecycle management
   - Alert fingerprinting and deduplication
   - Source: `tools/monitoring/alert-router/src/`
   - [OpenAPI Spec](/api/alert-router)

**Start Monitoring:**
```bash
docker compose -f tools/monitoring/docker-compose.yml up -d
```

**Access:**
- Prometheus: http://localhost:9090
- Grafana: http://localhost:3000 (admin/admin)
- Alertmanager: http://localhost:9093
- Alert Router: http://localhost:8080

---

### 🤖 AI & LLM Tools

#### Agno Agents (`tools/agno-agents/`)
AI agents built with Clean Architecture and Domain-Driven Design principles.

**Structure:**
- `src/application/` - Use cases and orchestration
- `src/domain/` - Business logic and entities
- `src/infrastructure/` - External integrations
- `src/interfaces/` - API endpoints and CLI
- `tests/` - Unit and integration tests

**Purpose:** Automated workflows, intelligent decision-making, task automation.

#### LangGraph (`tools/langgraph/`)
LangGraph integration for building stateful, multi-agent AI applications.

**Structure:**
- `src/application/` - LangGraph workflows
- `src/domain/` - Business entities
- `src/infrastructure/` - LLM providers, vector stores
- `src/interfaces/` - API and CLI
- `src/monitoring/` - Observability for LangGraph

**Purpose:** Complex multi-step AI workflows, agent orchestration.

#### LlamaIndex (`tools/llamaindex/`)
RAG (Retrieval-Augmented Generation) implementation using LlamaIndex.

**Services:**
- `ingestion_service/` - Document ingestion and indexing
- `query_service/` - Query processing and retrieval
- `k8s/` - Kubernetes deployments (production, staging)
- `tests/` - Service tests

**Purpose:** Document Q&A, semantic search, knowledge retrieval.

#### Ollama (`tools/ollama/`)
Local LLM runtime for running models like Llama 2, Mistral, CodeLlama without cloud dependencies.

**Purpose:** Local model inference, development without API costs, data privacy.

---

### 📦 Trading & Market Data

#### ProfitDLL (`tools/ProfitDLL/`)
**[Documented](/tools/profitdll/overview)** - Trading DLL for market data and order execution.

**Examples:**
- `Exemplo C#/` - C# integration examples
- `Exemplo C++/` - C++ integration examples
- `Exemplo Delphi/` - Delphi integration examples
- `Exemplo Python/` - Python integration examples
- `Manual/` - Official documentation (PT-BR and EN)

**Key Features:**
- Real-time market data streaming
- Order execution and management
- Multi-broker support
- WebSocket and callback-based APIs

---

### 🛠️ Development Tools

#### OpenSpec (`tools/openspec/`)
Specification management tool for tracking changes, proposals, and system design.

**Structure:**
- `changes/` - Change proposals (11 active proposals)
  - adapt-tp-capital-consume-gateway
  - add-telegram-gateway-dashboard
  - add-tp-capital-api-docs
  - containerize-tp-capital-workspace
  - database-tools-start-on-demand
  - implement-docs-api
  - migrate-docs-to-docusaurus-v2
  - optimize-docker-security-performance
  - split-tp-capital-into-gateway-api
  - unified-tp-capital-split-workspace-containerize
  - update-docs-apps
- `specs/` - System specifications (dashboard, docs-navigation, documentation-hosting, status)

**Purpose:** Structured change management, design documentation, proposal tracking.

#### Scripts (`tools/scripts/`)
Helper scripts for automation, maintenance, and operations.

**Categories:**
- `core/` - Core system operations
- `maintenance/` - Health checks, cleanup
- `setup/` - Installation and configuration
- `database/` - Database operations
- `docker/` - Docker management

#### Firecrawl Source (`tools/firecrawl/firecrawl-source/`)
Firecrawl open-source web scraping tool (cloned for local development).

**Purpose:** Web scraping, crawling, markdown conversion.

---

### 📖 Documentation Tools

Documented development tools for documentation system:

- **[Node.js & npm](/tools/node-npm/overview)** - JavaScript runtime and package manager
- **[Python](/tools/python/overview)** - Python runtime and virtual environments
- **[.NET SDK](/tools/dotnet/overview)** - C# development kit
- **[Docker & WSL](/tools/docker-wsl/overview)** - Container runtime
- **[Docusaurus](/tools/docusaurus/overview)** - Documentation site generator
- **[PlantUML](/tools/plantuml/overview)** - Diagram generation
- **[Redocusaurus](/tools/redocusaurus/overview)** - OpenAPI documentation
- **[Linting](/tools/linting/overview)** - Code quality tools

---

## 🗂️ Tool Categories Summary

| Category | Tools | Purpose |
|----------|-------|---------|
| **Infrastructure** | Docker Compose, Nginx Proxy, Registry, Systemd | Service orchestration and deployment |
| **Monitoring** | Prometheus, Grafana, Alertmanager, Alert Router | Observability and alerting |
| **AI/LLM** | Agno Agents, LangGraph, LlamaIndex, Ollama | AI workflows and local LLM inference |
| **Trading** | ProfitDLL | Market data and order execution |
| **Development** | OpenSpec, Scripts, Firecrawl | Change management and automation |
| **Documentation** | Node, Python, .NET, Docker, Docusaurus, PlantUML | Documentation toolchain |

---

## 🚀 Quick Start

### Start All Infrastructure
```bash
# 1. Start databases
docker compose -f tools/compose/docker-compose.database.yml up -d

# 2. Start monitoring
docker compose -f tools/monitoring/docker-compose.yml up -d

# 3. Start applications
docker compose -f tools/compose/docker-compose.apps.yml up -d

# 4. Verify health
bash scripts/maintenance/health-check-all.sh
```

### Access Services
- **Grafana**: http://localhost:3000
- **Prometheus**: http://localhost:9090
- **Adminer** (DB UI): http://localhost:8080
- **Alert Router**: http://localhost:8080
- **Documentation**: http://localhost:3205

---

## 📝 Tool Navigation

Use the sidebar to explore detailed documentation for each tool category:

- 🔧 **Development Tools** → Node, Python, .NET, Docker
- 📊 **Monitoring** → Prometheus, Grafana setup guides
- 🤖 **AI Tools** → Agno Agents, LangGraph, LlamaIndex
- 🔐 **Security & Config** → Environment variables, risk limits, audit
- 🚢 **Ports & Services** → Service port map and health checks
