---
title: Engineering Conventions
description: Coding conventions and folder structures for TradingSystem frontends.
tags:
  - frontend
  - engineering
  - conventions
owner: FrontendGuild
lastReviewed: '2025-10-26'
---
## Project Structure

- Apps live under `apps/<app-name>` with `src/` containing `components`, `hooks`, `store`, `utils`, and `types`.
- Shared assets reside in `frontend/shared`.

## Imports & Styling

- Prefer absolute imports via the configured Vite aliases (`@/components`, `@/utils`).
- Use the `cn` helper when composing Tailwind utility classes.
- Keep component-level styles colocated via CSS modules or Tailwind class composition.

## shadcn/ui Usage

- Install primitives via `npx shadcn-ui@latest add <component>`.
- Extend tokens in `frontend/dashboard/src/styles/theme.ts` and sync them into [Design Tokens](/frontend/design-system/tokens).

## Documentation

- Update PRDs and frontend docs together when introducing new user-visible behaviour.
- Reference ADRs for structural changes (TODO: link to frontend ADRs once migrated).
