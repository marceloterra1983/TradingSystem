# Placeholder schemas directory

This directory exists to satisfy tests that expect schema artifacts alongside the
OpenAPI/AsyncAPI specs. Add JSON or YAML schema files here when they become
available.

