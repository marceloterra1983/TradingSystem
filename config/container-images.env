# TradingSystem container image registry mapping
# ------------------------------------------------------------
# This file is versioned and provides the default image
# repositories for each service. Tags are controlled via
# IMG_VERSION (exported elsewhere or overridden per environment).
#
# To customize locally, define overrides in `.env.local` or export
# new values before running the stack scripts.
# ------------------------------------------------------------

# ---- TimescaleDB & tooling ----
IMG_DATA_TIMESCALEDB=timescale/timescaledb-ha
IMG_DATA_TIMESCALEDB_BACKUP=timescale/timescaledb-ha
IMG_DATA_TIMESCALEDB_EXPORTER=prometheuscommunity/postgres-exporter
IMG_DATA_TIMESCALEDB_PGADMIN=dpage/pgadmin4
IMG_DATA_TIMESCALEDB_PGWEB=sosedoff/pgweb
IMG_DATA_TIMESCALEDB_ADMINER=adminer
IMG_DATA_TIMESCALEDB_AZIMUTT=azimutt/azimutt

# ---- Core application images (replace with private registry if available) ----
IMG_DATA_FRONTEND_APPS=ghcr.io/tradingsystem/frontend-apps
IMG_DATA_POSTGRESS_LANGGRAPH=ghcr.io/tradingsystem/postgres-langgraph
IMG_DATA_QDRANT=qdrant/qdrant
IMG_DATA_QUESTDB=questdb/questdb

IMG_DOCS_API=ghcr.io/tradingsystem/docs-api
IMG_DOCS_API_VIEWER=ghcr.io/tradingsystem/docs-api-viewer

IMG_INFRA_LANGGRAPH=ghcr.io/tradingsystem/langgraph
IMG_INFRA_LLAMAINDEX_INGESTION=ghcr.io/tradingsystem/llamaindex-ingestion
IMG_INFRA_LLAMAINDEX_QUERY=ghcr.io/tradingsystem/llamaindex-query
IMG_INFRA_AGNO_AGENTS=ghcr.io/tradingsystem/agno-agents

# ---- Monitoring ----
IMG_MON_PROMETHEUS=prom/prometheus
IMG_MON_ALERTMANAGER=prom/alertmanager
IMG_MON_GRAFANA=grafana/grafana
IMG_MON_ALERT_ROUTER=ghcr.io/tradingsystem/alert-router
IMG_MON_NODE_EXPORTER=prom/node-exporter

# ---- Miscellaneous ----
IMG_OUTROS_CONTAINERS_REGISTRY=ghcr.io/tradingsystem/registry
IMG_OLLAMA=ollama/ollama

# ---- Firecrawl stack ----
IMG_FIRECRAWL_API=ghcr.io/tradingsystem/firecrawl-api
IMG_FIRECRAWL_PLAYWRIGHT=ghcr.io/tradingsystem/firecrawl-playwright
IMG_FIRECRAWL_POSTGRES=postgres
IMG_FIRECRAWL_REDIS=redis

