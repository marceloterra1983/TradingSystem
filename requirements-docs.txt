# ==============================================================================
# Health Dashboard Documentation Scripts - Python Dependencies
# ==============================================================================
# 
# Install all at once:
#   pip install -r requirements-docs.txt
#
# Install in user space (no sudo):
#   pip install --user -r requirements-docs.txt
#
# Upgrade existing packages:
#   pip install --upgrade -r requirements-docs.txt
#
# ==============================================================================

# ------------------------------------------------------------------------------
# Required Dependencies
# ------------------------------------------------------------------------------

# YAML parsing (for frontmatter validation)
PyYAML>=6.0

# HTTP requests (for link validation)
requests>=2.28.0

# ------------------------------------------------------------------------------
# Optional Dependencies (Recommended)
# ------------------------------------------------------------------------------

# Progress bars for better UX
tqdm>=4.64.0

# Environment variable management
python-dotenv>=0.20.0

# ------------------------------------------------------------------------------
# Development Dependencies (Optional)
# ------------------------------------------------------------------------------

# Testing
# pytest>=7.0.0
# pytest-cov>=3.0.0

# Code quality
# black>=22.0.0
# flake8>=4.0.0
# mypy>=0.950

# ------------------------------------------------------------------------------
# Notes
# ------------------------------------------------------------------------------
#
# 1. tqdm is optional but highly recommended - provides progress bars
#    Without it, scripts will still work but with less visual feedback
#
# 2. python-dotenv is optional - allows loading .env files
#    Without it, environment variables must be set manually
#
# 3. Development dependencies are commented out by default
#    Uncomment them if you plan to contribute to the scripts
#
# 4. All version numbers are minimum requirements
#    Newer versions should work fine
#
# ==============================================================================
