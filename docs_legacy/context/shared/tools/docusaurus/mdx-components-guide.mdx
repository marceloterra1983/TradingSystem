---
title: MDX Components Guide
description: Guide to using custom MDX components in TradingSystem Documentation
---

# MDX Components Guide

This guide shows how to use the custom React components available in the documentation.

## 1. Tabs Component

Use tabs for multi-language code examples:

import {Tabs, TabItem} from '@site/src/components/Tabs';

<Tabs>
  <TabItem value="python" label="Python">
    ```python
    # Initialize ProfitDLL
    from profit_dll import ProfitDLL

    dll = ProfitDLL()
    dll.initialize_login(key, username, password)
    ```
  </TabItem>
  <TabItem value="csharp" label="C#">
    ```csharp
    // Initialize ProfitDLL
    using ProfitDLL;

    var result = DLLInitializeLogin(key, username, password);
    ```
  </TabItem>
  <TabItem value="delphi" label="Delphi">
    ```pascal
    // Initialize ProfitDLL
    uses ProfitFunctionsU;

    result := DLLInitializeLogin(key, username, password);
    ```
  </TabItem>
</Tabs>

## 2. CodeBlock Component

Enhanced code blocks with titles and line numbers:

import CodeBlock from '@site/src/components/CodeBlock';

<CodeBlock language="typescript" title="server.ts" showLineNumbers>
{`import express from 'express';

const app = express();
const port = 3000;

app.get('/api/health', (req, res) => {
  res.json({ status: 'OK' });
});

app.listen(port, () => {
  console.log(\`Server running on port \${port}\`);
});`}
</CodeBlock>

## 3. ApiEndpoint Component

Document API endpoints with interactive examples:

import ApiEndpoint from '@site/src/components/ApiEndpoint';

<ApiEndpoint
  method="POST"
  path="/api/v1/execute"
  description="Execute a trading order with risk validation"
  queryParams={[
    { name: 'dry_run', type: 'boolean', description: 'Simulate order without execution', required: false }
  ]}
  requestBody={{
    symbol: "WINZ25",
    side: "BUY",
    quantity: 1,
    price: -1,
    position_type: 1
  }}
  responseBody={{
    order_id: "12345",
    status: "FILLED",
    filled_price: 119500,
    timestamp: "2025-10-10T02:30:00Z"
  }}
/>

<ApiEndpoint
  method="GET"
  path="/api/v1/positions"
  description="Get current trading positions"
  queryParams={[
    { name: 'symbol', type: 'string', description: 'Filter by symbol', required: false },
    { name: 'type', type: 'string', description: 'Filter by position type (day_trade or consolidated)', required: false }
  ]}
  responseBody={{
    positions: [
      {
        symbol: "WINZ25",
        quantity: 2,
        avg_price: 119450,
        current_price: 119500,
        pnl: 100.00,
        type: "day_trade"
      }
    ],
    total_pnl: 100.00
  }}
/>

## 4. Mermaid Diagrams

Create diagrams using Mermaid syntax:

### Architecture Diagram

\`\`\`mermaid
graph TD
    A[ProfitDLL] -->|Market Data| B[DataCapture]
    B -->|WebSocket| C[API Gateway]
    C -->|Orders| D[Order Manager]
    D -->|Executions| A
    C -->|Real-time| E[Dashboard]
    D -->|Positions| E

    style A fill:#6366f1,stroke:#4f46e5,color:#fff
    style B fill:#6366f1,stroke:#4f46e5,color:#fff
    style C fill:#06b6d4,stroke:#0891b2,color:#fff
    style D fill:#6366f1,stroke:#4f46e5,color:#fff
    style E fill:#10b981,stroke:#059669,color:#fff
\`\`\`

### Sequence Diagram

\`\`\`mermaid
sequenceDiagram
    participant P as ProfitDLL
    participant DC as DataCapture
    participant GW as API Gateway
    participant OM as OrderManager

    P->>DC: Trade Event
    DC->>GW: Publish via WebSocket
    GW->>OM: Forward Command
    OM->>OM: Risk Validation
    OM->>P: Execute Order
    P->>OM: Order Filled
    OM->>GW: Status Update
\`\`\`

### State Diagram

\`\`\`mermaid
stateDiagram-v2
    [*] --> Disconnected
    Disconnected --> Connecting: DLLInitializeLogin
    Connecting --> Connected: State 2 = Connected
    Connecting --> Error: Authentication Failed
    Connected --> Trading: Ready
    Trading --> Trading: Process Signals
    Trading --> RiskPause: Daily Loss Limit
    RiskPause --> Trading: Manual Resume
    Trading --> Disconnected: Disconnect
    Error --> [*]
    Disconnected --> [*]
\`\`\`

## Best Practices

### When to use each component:

- **Tabs**: Multi-language code examples, different configuration options
- **CodeBlock**: Standalone code with title and line numbers
- **ApiEndpoint**: REST API documentation with request/response examples
- **Mermaid**: Architecture diagrams, sequence flows, state machines

### Styling Tips:

- Keep diagrams simple and focused
- Use consistent colors (Indigo for backend, Cyan for analytics, Green for UI)
- Add titles to code blocks for clarity
- Include both request and response examples in API docs

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
